name: Preview Deployment

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      deploy:
        description: 'Deploy preview environment'
        type: boolean
        default: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/preview

jobs:
  build-preview:
    name: Build Preview App
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore .NET workloads
      run: dotnet workload restore

    - name: Restore dependencies
      run: dotnet restore

    - name: Build Solution
      run: dotnet build --no-restore --configuration Release

    - name: Test Core Library
      run: dotnet test NdcApp.Tests/NdcApp.Tests.csproj --no-build --configuration Release --verbosity normal

    - name: Log in to Container Registry
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      if: github.event_name != 'pull_request'
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./NdcApp.Preview/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

    - name: Build Docker image for PR
      if: github.event_name == 'pull_request'
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./NdcApp.Preview/Dockerfile
        push: false
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  deploy-preview:
    name: Deploy Preview Environment
    runs-on: ubuntu-latest
    needs: build-preview
    if: github.event_name != 'pull_request' && (github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    environment:
      name: preview
      url: http://preview.ndcapp.local:8080
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Update Preview Status Page
      run: |
        # Update PREVIEW_STATUS.md with current deployment info
        TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
        COMMIT_SHORT=${GITHUB_SHA:0:7}
        BRANCH_NAME=${GITHUB_REF_NAME}
        
        # Replace placeholders in PREVIEW_STATUS.md
        sed -i "s/\*\*Status:\*\* ⏳ Warten auf ersten Deployment/**Status:** ✅ Deployment erfolgreich/" PREVIEW_STATUS.md
        sed -i "s/\*\*Letztes Update:\*\* Noch kein Build/**Letztes Update:** $TIMESTAMP/" PREVIEW_STATUS.md
        sed -i "s/\*\*Preview URL:\*\* Noch nicht verfügbar/**Preview URL:** http:\/\/localhost:8080 (lokal) | http:\/\/[IHRE-IP]:8080 (iPhone)/" PREVIEW_STATUS.md
        sed -i "s/\*\*Branch:\*\* Noch kein Build/**Branch:** $BRANCH_NAME/" PREVIEW_STATUS.md
        sed -i "s/\*\*Commit:\*\* -/**Commit:** $COMMIT_SHORT/" PREVIEW_STATUS.md
        sed -i "s/\*\*Build Zeit:\*\* -/**Build Zeit:** $TIMESTAMP/" PREVIEW_STATUS.md
        sed -i "s/\*\*Docker Image:\*\* -/**Docker Image:** ${{ env.REGISTRY }}\/${{ env.IMAGE_NAME }}:$BRANCH_NAME/" PREVIEW_STATUS.md
        sed -i "s/\*\*Container Status:\*\* Noch nicht deployed/**Container Status:** Bereit für Deployment/" PREVIEW_STATUS.md
        sed -i "s/{{ TIMESTAMP }}/$TIMESTAMP/" PREVIEW_STATUS.md
        
        # Update the web status page
        sed -i "s/{{ TIMESTAMP }}/$TIMESTAMP/" NdcApp.Preview/wwwroot/status.html
      
    - name: Deploy with Docker Compose
      run: |
        echo "🚀 Preview deployment information:"
        echo "Docker image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}"
        echo "Build time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
        echo "Commit: ${GITHUB_SHA:0:7}"
        echo "Branch: ${{ github.ref_name }}"
        echo ""
        echo "📱 iPhone/Mobile Setup:"
        echo "1. Auf dem Computer: docker-compose -f docker-compose.preview.yml up -d"
        echo "2. IP-Adresse finden: ipconfig (Windows) oder ifconfig (macOS/Linux)"
        echo "3. iPhone Safari: http://[IHRE-IP]:8080"
        echo "4. Status-Seite: http://[IHRE-IP]:8080/status.html"
        echo ""
        echo "🔗 Preview Status Seite verfügbar unter:"
        echo "   - http://localhost:8080/status.html (lokal)"
        echo "   - http://[IHRE-IP]:8080/status.html (iPhone)"
        echo ""
        echo "✅ Preview erfolgreich erstellt und bereit für Deployment!"
        
    - name: Commit updated status
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add PREVIEW_STATUS.md NdcApp.Preview/wwwroot/status.html
        git diff --staged --quiet || git commit -m "🤖 Auto-update preview status after deployment [skip ci]"
        git push

  comment-pr:
    name: Comment on PR
    runs-on: ubuntu-latest
    needs: build-preview
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: write
      contents: read

    steps:
    - name: Comment PR
      uses: actions/github-script@v7
      with:
        script: |
          const { data: pullRequest } = await github.rest.pulls.get({
            owner: context.repo.owner,
            repo: context.repo.repo,
            pull_number: context.issue.number
          });

          const comment = `## 🎉 Preview App Built Successfully!

          ### NDC Conference Planner - Web Preview

          The preview version of your MAUI app has been built and is ready for testing!

          ### 📱 **NEUER iPhone Setup Guide verfügbar!**
          
          **Live Status Seite:** Nach dem Deployment verfügbar unter \`http://[IHRE-IP]:8080/status.html\`
          
          Diese Seite enthält:
          - ✅ **Schritt-für-Schritt iPhone Anleitung** mit QR-Code Generator
          - ✅ **Automatisches IP-Erkennung** für einfacheren Setup
          - ✅ **Interaktive Netzwerk-Tests** zur Diagnose
          - ✅ **Live Deployment Status** mit aktuellen Informationen
          - ✅ **Troubleshooting Guide** für häufige Probleme

          ### 🐳 Docker Deployment
          The preview app is containerized and ready to deploy. Use the following commands:

          \`\`\`bash
          # Build und Start (optimal für iPhone Zugang)
          docker-compose -f docker-compose.preview.yml up -d

          # Lokaler Zugang
          open http://localhost:8080
          
          # iPhone Setup Status Seite
          open http://localhost:8080/status.html
          
          # iPhone Zugang:
          # 1. IP-Adresse finden: ipconfig (Windows) / ifconfig (macOS/Linux)
          # 2. iPhone Safari: http://[IHRE-IP]:8080
          # 3. Status & Setup: http://[IHRE-IP]:8080/status.html
          \`\`\`

          ### 📱 iPhone Zugang - Vereinfacht
          1. **🔧 Computer vorbereiten:** \`docker-compose -f docker-compose.preview.yml up -d\`
          2. **📱 Status Seite öffnen:** \`http://[IP]:8080/status.html\`
          3. **📋 Anleitung folgen:** Interaktive Schritt-für-Schritt Anleitung
          4. **📸 QR Code nutzen:** Automatisch generiert für schnellen Zugang

          ### 🌟 Features Available
          - ✅ **Conference talk browsing** - View all available talks
          - ✅ **Personal planning** - Add/remove talks from your plan  
          - ✅ **Talk ratings** - Rate talks with 1-5 stars
          - ✅ **Filtering & search** - Filter by day, category, or search text
          - ✅ **Responsive design** - Works on desktop and mobile
          - ✅ **Real-time updates** - Live statistics and interactions
          - ✅ **NEW: Mobile Setup Guide** - Interactive iPhone setup assistance

          ### 📊 Deployment Status
          
          **Live Status:** [PREVIEW_STATUS.md](../PREVIEW_STATUS.md) - Automatisch aktualisiert nach jedem Build  
          **Interactive Guide:** \`http://[IHRE-IP]:8080/status.html\` - Nach Deployment verfügbar

          This web preview replicates the core functionality of your MAUI app for easy demonstration and testing purposes.

          ---
          *Built from commit: ${context.sha.substring(0, 7)}*
          *📱 iPhone Setup Guide: Jetzt verfügbar über Status-Seite*`;

          await github.rest.issues.createComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
            body: comment
          });